	class A(var a : Integer) is {
	def A(var a : Integer) is {
		a.toString().println();
	}

	def test(a : A) is {

	}
}

class A2(a : Integer) extends A is {
	def A2(a : Integer) : A(1) is {
		a.toString().println();
	}

}

{
	b : A := new A2(5);
	is
	b.test(new A2(5));
	b.a.toString().println();
}